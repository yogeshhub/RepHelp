language: node_js
node_js:
  - '4.4.3'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'IonicMedicalApp' ]; then cd .. && eval "mv $currentfolder IonicMedicalApp" && cd IonicMedicalApp; fi
